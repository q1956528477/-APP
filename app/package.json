{
  "name": "guanji",
  "version": "1.3.1",
  "description": "观己 - 身心状态记录",
  "private": true,
  "author": "",
  "scripts": {
    "build:bridges": "esbuild src/native.js --bundle --outfile=www/native.bundle.js && esbuild src/notify.js --bundle --outfile=www/notify.bundle.js",
    "test": "node test_app.js",
    "cap:add": "cap add android",
    "cap:sync": "cap sync android",
    "cap:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.3",
    "@capacitor/core": "^6.1.2",
    "@capacitor/filesystem": "^6.0.4",
    "@capacitor/local-notifications": "^6.1.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "esbuild": "^0.28.2"
  }
}
